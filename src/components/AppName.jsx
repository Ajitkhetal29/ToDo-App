import style from "./Appname.module.css";

const AppName = () => {
  return (
    <div className={style.appNameContainer}>
      <h4 className={style.appTitle}>ToDo App</h4>
    </div>
  );
};

export default AppName;
